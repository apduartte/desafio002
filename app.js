import (valida) from '.validacao.js'

const input = document.querySelectorAll('input')

inputs.forEach(Input=> {
    input.addEventListener(valida(input:any)):void
        valida(evento.target)
})
    
